/**
 * Deprecated: this module is currently unused by the server.
 *
 * Route-level validation is implemented inline in each `routes/*` file using
 * `express-validator` (CommonJS). This file previously used ESM exports which
 * are incompatible with the rest of this codebase.
 */

module.exports = {};
